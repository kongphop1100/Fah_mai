# FahMai team-agent — runtime deps (pinned from uv.lock).
# For the full dev env (notebooks, embeddings ingestion) use `uv sync` instead.
#   pip install -r requirements.txt   &&   pip install -e .
langgraph==1.2.2
langchain-openai==1.2.2
langchain-core==1.4.0
langsmith==0.8.8
pandas==3.0.3
sqlalchemy==2.0.50
psycopg[binary]==3.3.4
pythainlp>=5.0.0
python-dotenv==1.2.2
httpx
